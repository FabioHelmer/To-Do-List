spring:
  datasource:
    url: jdbc:sqlserver://localhost:1433;databaseName=db_todolist;encrypt=true;trustServerCertificate=true
    username: sa
    password: 123Mudar
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
    show-sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    baseline-version: 0

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method
    tags-sorter: alpha
  api-docs:
    path: /v3/api-docs